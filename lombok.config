config.stopBubbling = true

# copy jspecify annotations to generated elements
lombok.copyableAnnotations += org.jspecify.annotations.Nullable
lombok.copyableAnnotations += org.jspecify.annotations.NonNull
